<footer id="footer" class="pt-5">
    <div class="container-fluid">
        <div class="row mb-5">
            <div class="col-12 col-sm-5">
                <div class="subscribe input-group mb-3">
                    <input type="text" class="form-control" placeholder="name@email.com">
                    <div class="input-group-append">
                        <button class="btn btn-outline-secondary" type="button">Subscribe</button>
                    </div>
                </div>
                <p class="small">Join our newsletter. Be the first to receive the latest updates about news collections, stories and much more.</p>

            </div>
            <div class="col-12 col-sm-7">
                <div class="d-flex justify-content-between justify-content-md-center flex-row">
                    {% for i in range(0,3) %}
                    <div class="foot-menu mobile-mr-3 mr-5">
                        <h6>NUETA</h6>
                        <nav class="nav flex-column small">
                            <a class="" href="#">Mission &amp; Vision</a>
                            <a class="" href="#">Project References</a>
                            <a class="" href="#">FAQ</a>
                            <a class="" href="#">Career</a>
                            <a href="mailto:cs@nueta.id" class="mt-4">cs@nueta.id</a>
                        </nav>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>

        <div class="d-flex flex-column flex-md-row justify-content-between py-4 border-top">
            <div class="d-flex flex-row">
                {% for i in range(0,3) %}
                <a class="small mr-3" href="#">Footer menu {{i}}</a>
                {% endfor %}
            </div>
            <div class="mobile-mt-3">
                <span class="small">&copy; NUETA 2020 &mdash; Site by <a href="https://www.mineral.co.id/">Mineral</a> </span>
            </div>
        </div>
    </div>
</footer>